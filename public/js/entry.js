import Router from './modules/router.js'

(async () => {
    try {
        var i;
        if (true) {

        }
        Router.init();
    } catch (err) {
        console.error(err);
        // Ошибка
    }
})();